pt-BR:
  active_admin:
    dashboard: 'Painel'
    filters:
      predicates:
        from: de
        to: até
  activerecord:
    models:
      active_admin/comment:
        zero: 'comentário'
        one: 'comentário'
        other: 'comentários'
